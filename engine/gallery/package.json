{
  "name": "@facet/gallery",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Facet gallery — every widget side-by-side in both backends (SVG vs cells). The parity showcase + visual eval.",
  "dependencies": {
    "@facet/core": "0.0.0",
    "@facet/render-dom": "0.0.0",
    "@facet/render-term": "0.0.0"
  },
  "scripts": {
    "build": "tsx src/build.ts",
    "storybook": "tsx src/storybook-cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
