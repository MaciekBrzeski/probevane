{
  "name": "@facet/core",
  "version": "0.0.0",
  "type": "module",
  "description": "Facet core — pure vector Painter interface, Style + colour/anim math, geometry, layered-DAG layout, and widgets. No DOM, no ANSI.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "coverage": "vitest run --coverage"
  }
}
